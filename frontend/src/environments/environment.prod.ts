export const environment = {
  production: true,
  // Cambia esta URL por la URL de producción de tu backend una vez que esté desplegado (ej. Render, Railway, etc.)
  apiUrl: 'http://localhost:8080/api'
};
